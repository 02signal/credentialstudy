# Agent Entry Point

Read `CLAUDE.md` first — it is the single source of truth for this repository.
Read `CLAUDE.local.md` (gitignored) before any public change — it holds the embargo and
deploy constraints that override everything.

Before planning or finishing work, also check `BACKLOG.md`.

Do not deploy, connect domains, change analytics IDs, or announce specific programmes
unless the owner explicitly asks for it. This is the provider/academy brand; keep it
strictly separate from the neutral register (`mikrokvalifikatsioon.ee`).
