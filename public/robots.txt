# PRE-LAUNCH / EMBARGO (see CLAUDE.local.md): the site is a skeleton and must
# stay out of search until the owner lifts the embargo. Keep all crawlers out.
# At launch, restore the allow-list version (sitemap + AI bots) from git history.
User-agent: *
Disallow: /
